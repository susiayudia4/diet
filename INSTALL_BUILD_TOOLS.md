# 🔧 Решение проблемы установки better-sqlite3

## Проблема
При установке зависимостей возникает ошибка компиляции `better-sqlite3`, которая требует Visual Studio Build Tools.

## ✅ Решение 1: Установить Visual Studio Build Tools (Рекомендуется)

### Шаги:

1. **Откройте браузер и перейдите:**
   ```
   https://visualstudio.microsoft.com/downloads/
   ```

2. **Прокрутите вниз до раздела "Tools for Visual Studio"**

3. **Скачайте "Build Tools for Visual Studio"**

4. **Запустите установщик**

5. **В установщике выберите:**
   - ✅ **"Desktop development with C++"**
   - Это установит необходимые компиляторы C++

6. **Нажмите "Install"** и дождитесь завершения (может занять 5-10 минут)

7. **Перезапустите `start.bat`**

---

## ✅ Решение 2: Использовать Node.js 20 LTS

Если у вас установлен Node.js 24.x, можно перейти на Node.js 20 LTS, для которого есть предкомпилированные бинарники.

### Шаги:

1. **Скачайте Node.js 20 LTS:**
   ```
   https://nodejs.org/
   ```

2. **Установите Node.js 20.x**

3. **Перезапустите `start.bat`**

---

## ✅ Решение 3: Использовать альтернативную базу данных

Если установка Build Tools невозможна, можно заменить `better-sqlite3` на другую библиотеку, но это потребует изменения кода.

---

## 📝 Примечания

- Visual Studio Build Tools занимает около 3-6 ГБ места на диске
- Установка может занять 5-15 минут
- После установки Build Tools, все будущие проекты с нативными модулями будут работать без проблем

---

## ❓ Вопросы?

Если проблема сохраняется после установки Build Tools:
1. Перезагрузите компьютер
2. Убедитесь, что установлен компонент "Desktop development with C++"
3. Попробуйте удалить `node_modules` и `package-lock.json`, затем запустите `start.bat` снова


