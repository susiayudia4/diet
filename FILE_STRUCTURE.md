# 📂 CalorieTracker - Полная файловая структура

## 📋 Список всех файлов проекта

### 🚀 Root файлы (Главная папка)

```
MAX/
├── server.js ......................... Express сервер (105 строк)
├── package.json ...................... Dependencies (14 зависимостей)
├── .env .............................. Environment variables
├── .gitignore ........................ Git ignore file
├── start.bat ......................... Запуск на Windows ✓
├── start.sh .......................... Запуск на Unix ✓
│
├── README.md ......................... Основное описание
├── QUICKSTART.md ..................... Быстрый старт за 5 минут
├── INSTRUCTIONS.md ................... Полная инструкция (на русском)
├── API_EXAMPLES.md ................... Примеры использования API
├── PROJECT_INFO.md ................... Информация о проекте
├── FILE_STRUCTURE.md ................. Этот файл
│
├── public/ ........................... Frontend файлы
│   ├── index.html .................... HTML template (130 строк)
│   ├── styles.css .................... Стили (650+ строк)
│   └── app.js ........................ Главное приложение (700+ строк)
│
└── src/ .............................. Backend исходный код
    ├── db/ ........................... Слой базы данных
    │   ├── database.js ............... Инициализация БД (110 строк)
    │   └── seed.js ................... Тестовые данные (100 строк)
    │
    ├── middleware/ ................... Middleware функции
    │   └── auth.js ................... JWT + RBAC (30 строк)
    │
    ├── controllers/ .................. Бизнес логика
    │   ├── authController.js ......... Аутентификация (64 строк)
    │   ├── productController.js ...... Продукты CRUD (45 строк)
    │   ├── mealController.js ......... Приёмы пищи (100+ строк)
    │   └── statsController.js ........ Статистика (50+ строк)
    │
    └── routes/ ....................... API маршруты
        ├── authRoutes.js ............ Маршруты аутентификации
        ├── productRoutes.js ......... Маршруты продуктов
        ├── mealRoutes.js ............ Маршруты приёмов пищи
        └── statsRoutes.js ........... Маршруты статистики
```

---

## 📄 Описание каждого файла

### 🔧 Конфигурационные файлы

#### `server.js` (105 строк)
**Назначение:** Главный файл сервера Express
**Содержит:**
- Инициализация Express приложения
- Подключение middleware (CORS, JSON parser)
- Инициализация БД и seed данных
- Монтирование всех маршрутов
- Swagger UI конфигурация
- Обработка ошибок
- Запуск сервера на порту 3000

**Ключевые функции:**
```javascript
// Инициализация
app.use(cors())
app.use(express.json())
app.use(express.static('public'))

// Монтирование маршрутов
app.use('/api/auth', authRoutes)
app.use('/api/products', productRoutes)
app.use('/api/meals', mealRoutes)
app.use('/api/stats', statsRoutes)

// Запуск
app.listen(PORT, ...)
```

#### `package.json` (28 строк)
**Назначение:** Управление зависимостями Node.js
**Содержит:**
- Метаинформация проекта
- 14 production dependencies
- npm скрипты для запуска
- Лицензия (MIT)

**Зависимости:**
```json
"dependencies": {
  "express": "^4.18.2",
  "cors": "^2.8.5",
  "better-sqlite3": "^9.0.0",
  "jsonwebtoken": "^9.1.0",
  "bcryptjs": "^2.4.3",
  "dotenv": "^16.3.1",
  "swagger-ui-express": "^5.0.0",
  "swagger-jsdoc": "^6.2.8"
}
```

#### `.env` (4 строки)
**Назначение:** Переменные окружения
**Содержит:**
```
PORT=3000
JWT_SECRET=your-secret-key
NODE_ENV=development
```

#### `.gitignore`
**Назначение:** Исключает файлы из git
**Содержит:**
```
node_modules/
.env
*.db
.DS_Store
```

#### `start.bat` (Windows)
**Назначение:** Батник для быстрого запуска на Windows
**Функции:**
- Проверка Node.js установки
- Автоматическая установка dependencies
- Запуск сервера
- Вывод информации о доступе

#### `start.sh` (Unix/Mac)
**Назначение:** Shell скрипт для запуска на macOS/Linux
**Функции:**
- Аналогично start.bat
- Использует bash вместо bat

---

### 🌐 Frontend файлы (public/)

#### `index.html` (130 строк)
**Назначение:** HTML template приложения
**Содержит:**
- Базовую HTML структуру
- Мета теги для адаптивности
- Подключение CSS и JavaScript
- Div контейнер для приложения

**Структура:**
```html
<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CalorieTracker</title>
    <link rel="stylesheet" href="/styles.css">
    <style>/* Additional styles */</style>
  </head>
  <body>
    <div id="app"></div>
    <script src="/app.js"></script>
  </body>
</html>
```

#### `styles.css` (650+ строк)
**Назначение:** Все стили приложения
**Секции:**
1. **CSS Variables** - Переменные цветов и размеров
2. **Typography** - Шрифты и текст
3. **Navigation** - Меню и заголовок
4. **Forms** - Форма для ввода данных
5. **Cards** - Карточки компонентов
6. **Buttons** - Стили кнопок
7. **Modals** - Всплывающие окна
8. **Animations** - Анимации и переходы
9. **Responsive** - Адаптив для мобильных
10. **Dashboard** - Стили главной панели

**Особенности:**
- Градиенты и тени
- Flexbox и CSS Grid
- Hover эффекты
- Smooth переходы
- Mobile-first дизайн

#### `app.js` (700+ строк)
**Назначение:** Главное приложение (Single Page Application)
**Структура:**

1. **Утилиты (`utils`)**
   - `getToken()` - Получить JWT токен
   - `setToken()` - Сохранить токен
   - `showAlert()` - Показать уведомление
   - `formatDate()` - Форматировать дату

2. **API функции (`api`)**
   - `api.call()` - Базовый fetch wrapper
   - `api.auth` - Auth endpoints
   - `api.products` - Products endpoints
   - `api.meals` - Meals endpoints
   - `api.stats` - Stats endpoints

3. **UI функции (`ui`)**
   - `showPage()` - Переключение страниц
   - `updateHeader()` - Обновление навигации
   - `navigateTo()` - Переход на страницу

4. **Auth функции (`auth`)**
   - `login()` - Вход в систему
   - `register()` - Регистрация
   - `logout()` - Выход

5. **Страницы (`pages`)**
   - `pages.dashboard` - Главная панель
   - `pages.diary` - Дневник питания
   - `pages.products` - Библиотека продуктов
   - `pages.stats` - Статистика
   - `pages.profile` - Профиль пользователя
   - `pages.login` - Страница входа
   - `pages.register` - Страница регистрации

---

### 🛠️ Backend файлы (src/)

#### `src/db/database.js` (110 строк)
**Назначение:** Инициализация SQLite базы данных
**Создаёт таблицы:**
- `users` - Пользователи
- `products` - Продукты
- `recipes` - Рецепты
- `recipe_items` - Ингредиенты рецептов
- `meal_entries` - Приёмы пищи
- `goals` - Цели пользователей
- `consultations` - Консультации

**Функции:**
```javascript
export function initializeDatabase() {
  // Проверка файла БД
  // Создание таблиц если нужно
  // Создание индексов
  return db // объект базы
}

export function getDatabase() {
  return db
}
```

#### `src/db/seed.js` (100+ строк)
**Назначение:** Загрузка тестовых данных
**Содержит:**
- 4 тестовых пользователя
- 10 встроенных продуктов
- Примеры приёмов пищи
- Примеры целей

**Функция:**
```javascript
export function seedDatabase(db) {
  // Добавляет всё необходимое для демонстрации
}
```

#### `src/middleware/auth.js` (30 строк)
**Назначение:** Аутентификация и авторизация
**Функции:**
```javascript
export function authenticateToken(req, res, next) {
  // Проверка JWT токена в заголовке
  // Извлечение user информации
}

export function authorize(...roles) {
  // Проверка роли пользователя
  // Разрешение доступа если совпадает
}
```

#### `src/controllers/authController.js` (64 строк)
**Назначение:** Логика аутентификации
**Функции:**
- `register` - POST /api/auth/register
- `login` - POST /api/auth/login
- `getMe` - GET /api/auth/me
- `updateProfile` - PUT /api/auth/profile

**Примеры:**
```javascript
export async function register(req, res) {
  const { username, email, password, role } = req.body
  // Валидация
  // Хеширование пароля
  // Сохранение в БД
  // Возврат токена
}

export async function login(req, res) {
  const { email, password } = req.body
  // Поиск пользователя
  // Проверка пароля
  // Генерация токена
  // Возврат токена и пользователя
}
```

#### `src/controllers/productController.js` (45 строк)
**Назначение:** Управление продуктами
**Функции:**
- `getAll` - GET /api/products
- `create` - POST /api/products
- `delete` - DELETE /api/products/:id

**Примеры:**
```javascript
export async function getAll(req, res) {
  // Получить встроенные продукты
  // Получить пользовательские продукты
  // Вернуть объединённый список
}

export async function create(req, res) {
  const { name, calories, protein, carbs, fats } = req.body
  // Валидация
  // Сохранение в БД
  // Возврат созданного продукта
}
```

#### `src/controllers/mealController.js` (100+ строк)
**Назначение:** Управление приёмами пищи
**Функции:**
- `getByDate` - GET /api/meals?date=...
- `add` - POST /api/meals
- `delete` - DELETE /api/meals/:id

**Примеры:**
```javascript
export async function getByDate(req, res) {
  const { date } = req.query
  // Получить все приёмы пищи на дату
  // Сгруппировать по типам
  // Рассчитать макронутриенты
  // Вернуть статистику
}

export async function add(req, res) {
  const { product_id, meal_type, quantity, date } = req.body
  // Валидация
  // Создание записи
  // Возврат созданной записи
}
```

#### `src/controllers/statsController.js` (50+ строк)
**Назначение:** Расчёт статистики
**Функции:**
- `getWeekly` - GET /api/stats/weekly
- `getMonthly` - GET /api/stats/monthly

**Примеры:**
```javascript
export async function getWeekly(req, res) {
  // Получить данные за 7 дней
  // Рассчитать по дням
  // Вернуть массив статистики
}

export async function getMonthly(req, res) {
  // Получить данные за 30 дней
  // Рассчитать по дням
  // Вернуть объединённую статистику
}
```

---

### 🛣️ Routes файлы (src/routes/)

#### `src/routes/authRoutes.js`
**Маршруты:**
```javascript
router.post('/register', authController.register)
router.post('/login', authController.login)
router.get('/me', authenticateToken, authController.getMe)
router.put('/profile', authenticateToken, authController.updateProfile)
```

#### `src/routes/productRoutes.js`
**Маршруты:**
```javascript
router.get('/', authenticateToken, productController.getAll)
router.post('/', authenticateToken, productController.create)
router.delete('/:id', authenticateToken, productController.delete)
```

#### `src/routes/mealRoutes.js`
**Маршруты:**
```javascript
router.get('/', authenticateToken, mealController.getByDate)
router.post('/', authenticateToken, mealController.add)
router.delete('/:id', authenticateToken, mealController.delete)
```

#### `src/routes/statsRoutes.js`
**Маршруты:**
```javascript
router.get('/weekly', authenticateToken, statsController.getWeekly)
router.get('/monthly', authenticateToken, statsController.getMonthly)
```

---

### 📚 Документация файлы

#### `README.md`
- Основное описание проекта
- Особенности
- Быстрый старт
- Структура файлов
- Требования
- Технологический стек

#### `QUICKSTART.md`
- Запуск на Windows
- Запуск на Mac/Linux
- Первый вход
- Тестовые аккаунты
- Советы

#### `INSTRUCTIONS.md` (на русском)
- Полная инструкция по использованию
- Описание каждой функции
- Советы и трюки
- FAQ
- Решение проблем

#### `API_EXAMPLES.md`
- Примеры curl запросов
- Примеры JavaScript
- Структура ответов API
- Коды ошибок

#### `PROJECT_INFO.md`
- Информация о проекте
- Архитектура
- Технологический стек
- Структура БД
- Статистика проекта

#### `FILE_STRUCTURE.md`
- Этот файл
- Описание каждого файла
- Назначение каждого модуля

---

## 📊 Статистика файлов

| Категория | Файлы | Строк кода | Размер |
|-----------|-------|-----------|---------|
| Backend | 8 файлов | ~600 | ~25KB |
| Frontend | 3 файла | ~1400 | ~60KB |
| Config | 3 файла | ~45 | ~2KB |
| Docs | 6 файлов | ~1200 | ~80KB |
| **Всего** | **20 файлов** | **~3245** | **~167KB** |

---

## 🔄 Взаимосвязь файлов

```
server.js
├── src/db/database.js (инициализация БД)
├── src/db/seed.js (загрузка данных)
├── src/routes/* (подключение маршрутов)
│   ├── authRoutes.js
│   ├── productRoutes.js
│   ├── mealRoutes.js
│   └── statsRoutes.js
└── src/middleware/auth.js (защита маршрутов)

src/routes/*.js
├── controllers/*.js (обработка запросов)
└── middleware/auth.js (проверка прав)

public/app.js
├── index.html (загружается в)
└── styles.css (использует стили из)

controllers/*.js
└── src/db/database.js (работает с БД через)
```

---

## 🎯 Что находится в каждом файле

| Когда нужно | Открой файл |
|-------------|------------|
| Запустить приложение | `server.js` или `start.bat`/`start.sh` |
| Изменить стили | `public/styles.css` |
| Добавить функцию в UI | `public/app.js` |
| Добавить API endpoint | `src/routes/*.js` + `src/controllers/*.js` |
| Изменить структуру БД | `src/db/database.js` |
| Добавить тестовые данные | `src/db/seed.js` |
| Разобраться как использовать | `README.md` или `INSTRUCTIONS.md` |
| Посмотреть примеры API | `API_EXAMPLES.md` |

---

**Все файлы готовы к использованию! ✅**
